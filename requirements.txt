# Внешние зависимости проекта.
# Pillow — нужна только picher/digitize.py (картинка → схема редактора);
# glitch.py, belt_rus.py и picher/render.py работают на чистой
# стандартной библиотеке.
# Flask — веб-интерфейс web/app.py (запуск генераторов из браузера);
# при системном Python без pip: sudo apt install python3-flask.
#
# Установка: bash setup.sh (Linux/macOS) или setup.cmd (Windows) из корня.
Pillow>=9.0
Flask>=2.0
