fix(patterns): переменная ширина букв шрифта в glitch.py

Шрифт type.txt расширен вручную: O, T, V, W, X, Y — 8 колонок,
Q — 9, остальные 63 глифа — по 7. Скрипт падал («нужно 7 цифр
из 0-5»). Теперь ширина глифа — любая, из самого файла:

- GLYPH_W удалён; фиксирована только высота GLYPH_H = 6;
- load_font: принимает глиф любой ширины, требует лишь одинаковой
  длины всех 6 рядов и цифр 0-5; понятные ошибки на кривые ряды
  («ряды глифа разной ширины (7 и 8)») и чужие символы;
- text_width(text, font, tracking) суммирует фактические ширины
  глифов (было 7 × длина текста); pick_tracking и build_grid
  получают шрифт;
- обновлены докстринги, шпаргалка help, README.md и шапка-комментарий
  type.txt (комментарий, парсером пропускается).

Проверено: 70 глифов (63×7 + OTVWXY×8 + Q×9); растр «BOGDANNA» =
64 колонки; команда -t bracelet_19 --text "BOGDANNA" даёт понятное
«не влезает» (64 > 42; для этой надписи нужно --cols от 66);
--cols 80 и кириллица «ГЛИТЧ» — успех; два запуска с одним seed —
идентичный HTML; кривые тест-шрифты — внятные ошибки; help работает.
