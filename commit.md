feat(patterns): многоцветный шрифт type.txt + генератор схем на нём

Новый шрифт patterns/type.txt: пиксельный 7×6, каждая клетка — цифра
цвета 0-5 (0 — чёрный фон, 1 — пурпурный, 2 — жёлтый, 3 — белый,
4 — голубой, 5 — синий). Цветной «шлейф» зашит в сами буквы: слева
пурпурный/жёлтый, центр белый, справа голубой/синий. 70 глифов:
латиница, кириллица с Ё, цифры, пробел (метка «_»); без знаков
препинания и строчных. A и B из наброска исправлены — залитые цветами
просветы заменены на 0-фон.

glitch_belt.py:

- шрифт читается из type.txt при каждом запуске (--font): правка букв
  не требует правок кода; валидация глифов с понятными ошибками;
- растр 7×6 вместо 5×7, минимум ширины пояса — 6 рядов;
- цвет пикселя буквы = цифра шрифта = № коробочки 0-5;
- rgb-шлейф и --rgb-shift удалены (шлейф теперь в самом шрифте);
  --glitch — сила искажений --dropout/--slices/--blackout;
- palette.json — 6 цветов «RGB Split» (0 чёрный фон … 5 синий),
  hex и name правятся под свой бисер.

README переписан под новый шрифт и палитру; примеры схем
в patterns/out/ перегенерированы.

Фикс печати: сегменты считались от полной печатной ширины (SVG 193 мм
из 194 доступных при CSS-полях 8 мм) — при полях браузера/принтера
больше 8 мм правый край сетки выходил за лист. Теперь ширина сегментов
считается от консервативных 182 мм (влезает при любых полях до ~13 мм),
вертикальный бюджет сегмента честный (линия отреза 8.8 мм, отбивка 1 мм,
заголовок 4.3 мм) — страницы не переполняются.

Система пресетов: patterns/config.json (вместо palette.json) — пресеты
belt_90 / bracelet_19 / bracelet_15, каждый задаёт размер полотна
(length_cm, width_cm), шаг между центрами кружочков (pitch_mm), диаметр
кружочка (circle_d_mm), файл шрифта (font, отдельный — правится руками)
и 6 цветов (colors = цифры шрифта 0-5 = номера коробочек). Загрузка:
-c/--config и -t/--preset; флаги CLI переопределяют поля пресета;
--palette и --circle-gap убраны, вместо зазора — прямой
--circle-d-mm; без -t работают прежние дефолты (запасная палитра
встроена в скрипт).

Размер полотна — в клетках: --cols (длина-колонки) и --rows
(ширина-ряды), в конфиге поля cols/rows; физический размер знают
только кружочки (pitch_mm — расстояние между центрами, circle_d_mm —
диаметр). Сантиметровых флагов --length-cm/--width-cm больше нет,
мм-размер изделия выводится производно (клетки × шаг) в консоль
и шапку схемы. Пресеты пересчитаны под шаг 4.5 мм / кружок 3 мм:
belt_90 → 200×9, bracelet_19 → 42×9, bracelet_15 → 33×9.

Проверено: полный алфавит (70 глифов, все валидны), ASCII-превью,
детерминизм по seed, счёт бисерин = размеру сетки, понятная ошибка
на неизвестном символе, строчные → ЗАГЛАВНЫЕ; сегменты ≤ 181 мм,
при шагах 2.0–4.0 мм ширина не выходит за печатную область;
пресеты/переопределения/ошибки конфига проверены запусками.

