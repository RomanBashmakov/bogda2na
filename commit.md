feat(patterns): многоцветный шрифт type.txt + генератор схем на нём

Новый шрифт patterns/type.txt: пиксельный 7×6, каждая клетка — цифра
цвета 0-5 (0 — чёрный фон, 1 — пурпурный, 2 — жёлтый, 3 — белый,
4 — голубой, 5 — синий). Цветной «шлейф» зашит в сами буквы: слева
пурпурный/жёлтый, центр белый, справа голубой/синий. 70 глифов:
латиница, кириллица с Ё, цифры, пробел (метка «_»); без знаков
препинания и строчных. A и B из наброска исправлены — залитые цветами
просветы заменены на 0-фон.

glitch_belt.py:

- шрифт читается из type.txt при каждом запуске (--font): правка букв
  не требует правок кода; валидация глифов с понятными ошибками;
- растр 7×6 вместо 5×7, минимум ширины пояса — 6 рядов;
- цвет пикселя буквы = цифра шрифта = № коробочки 0-5;
- rgb-шлейф и --rgb-shift удалены (шлейф теперь в самом шрифте);
  --glitch — сила искажений --dropout/--slices/--blackout;
- palette.json — 6 цветов «RGB Split» (0 чёрный фон … 5 синий),
  hex и name правятся под свой бисер.

README переписан под новый шрифт и палитру; примеры схем
в patterns/out/ перегенерированы.

Проверено: полный алфавит (70 глифов, все валидны), ASCII-превью,
детерминизм по seed, счёт бисерин = размеру сетки, понятная ошибка
на неизвестном символе, строчные → ЗАГЛАВНЫЕ.

